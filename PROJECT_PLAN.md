# خطة تدقيق وتقوية مشروع مهووس — من البداية إلى النهاية

> اقرأ `CLAUDE.md` أولاً (الدليل + البروتوكول الصارم). هذه الخطة بمراحل. **مرحلة واحدة لكل جلسة**
> (لإبقاء الجلسات قصيرة ورخيصة ودقيقة). بعد كل مرحلة: حدّث قسم «📊 الحالة» في أسفل هذا الملف.

## المبادئ (غير قابلة للتفاوض)
- **لا فقدان بيانات.** خذ نسخة احتياطية من `data/pricing_v18.db` قبل أي تغيير يمسّ القاعدة.
- **لا أخطاء جديدة.** تحقّق (AST + import) قبل كل commit. ملف واحد لكل commit.
- **لا وعود زائفة.** الهدف صحّة **قابلة للقياس** (اختبارات + فحص ثوابت)، لا «صفر أخطاء» شعاراً.
- **المستخدم يدفع ويتحقق على Railway** بعد كل commit. اعرض التشخيص واطلب الموافقة قبل التعديل.
- **عند الشك → مراجعة**، لا حذف ولا إخفاء صامت.

## طريقة العمل لكل مرحلة
1. **تشخيص (قراءة فقط):** افحص، اعرض النتائج بأرقام/أمثلة، لا تعدّل كوداً.
2. **موافقة المستخدم** على ما سيُصلَح.
3. **إصلاح:** تغيير واحد منطقي = ملف واحد = commit، مع تحقّق محلي.
4. **المستخدم يدفع + يتحقق على Railway** → يؤكد «يعمل».
5. **حدّث «📊 الحالة»** ثم انتقل.

---

## المرحلة 0 — شبكة الأمان + التدقيق الشامل (قراءة فقط)
- [ ] نسخة احتياطية: `cp data/pricing_v18.db data/pricing_v18.backup.db` (وأخبر المستخدم أين هي).
- [ ] شغّل الاختبارات الحالية: `pytest tests/ -q` — سجّل ما يمر/يفشل.
- [ ] شغّل فحص الثابت (`observability/ledger.py`) على عيّنة — هل تُفقد صفوف؟
- [ ] شغّل `tests/baseline_missing.py` — سجّل الأرقام الحالية (مفقود/مراجعة/إيجابيات كاذبة) كخط أساس.
- [ ] جرد المشاكل: ابحث عن `except: pass`/`except Exception: pass` (إسقاط صامت)، `print(` بدل logging، أرقام سحرية، استيرادات غير مستخدمة، `TODO/FIXME`.
- [ ] **اعرض تقرير تشخيص** مرتّب بالأولوية (خطورة/أثر) قبل أي إصلاح.
- **تمّ عندما:** يوجد نسخة احتياطية + تقرير تشخيص + خط أساس مقيس.

## المرحلة 1 — سلامة البيانات والأداء عند الحجم (الأهم لمئات الآلاف + 10 منافسين)
> هذا أكبر خطر: تحميل كل البيانات + مطابقة في بايثون = OOM/تعليق على Railway.
- [x] فهارس DB على `competitor_products_store` — **مُنجَز L2**: نُظّفت 16→8 (لا مكرّرات)، 3 مصادر موحّدة، إدخال أسرع 38%.
- [x] قراءة دفعات (chunksize) في مسار المفقودات — **مُنجَز H1** (fetchmany). التصدير (`export_missing.py` أوفلاين) مؤجَّل.
- [ ] حارس ذاكرة + cache قرصي للمفقودات (موجود جزئياً: `missing_cache.pkl`) — تأكد أنه يصمد عند الحجم.
- [x] قياس زمن/ذاكرة المفقودات على البيانات الكاملة — **مُنجَز** (129,644 صف: ~8s، ذروة RSS 317→290MB بعد H1).
- [ ] تأكد أن إضافة 10 منافسين + مئات الآلاف لا تكسر الإقلاع (لا حساب ثقيل عند الإقلاع).
- **تمّ عندما:** المسارات الثقيلة بدفعات + فهارس، والإقلاع خفيف، وقياس الأداء مقبول.

## المرحلة 2 — اتساق المطابقة وإزالة التكرار
- [ ] **مُطبِّع واحد للمطابقة في كل المشروع = `miss_bare`** (لا `_norm_dup_text` ولا تطبيع ضعيف). جرد كل مواضع المطابقة ووحّدها.
- [ ] قسم المفقودات: تأكد من classify الثلاثي + الهيكل العظمي + حارس الحجم (مُنجز معظمه — راجعه).
- [ ] `verify_truly_missing` (تصدير سلة): قوِّ تطبيعه ليساوي `miss_bare`.
- [ ] فحص تكرار «المعالجة الذكية»: مُصلَح (يستخدم miss_bare) — تأكد، واجعل «استبعاد تلقائي» غير افتراضي.
- **تمّ عندما:** مطابقة واحدة متّسقة، صفر إيجابيات كاذبة جماعية، التكرار الحقيقي وحده يُكتشف.

## المرحلة 3 — صحّة تحليل الأسعار
- [ ] راجع `engine.py` تصنيف (أعلى/أقل/موافق/مراجعة/مستبعد) — العتبات، لا إسقاط صامت، لا تكرار بين الأقسام.
- [ ] تأكد أن كل قسم يُصفّي المُعالَج بمفتاح مستقر (معرّف المنتج)، والمُرسَل لا يعود (مُصلَح — راجعه).
- [ ] فحص الثابت: مجموع الأقسام = إجمالي المُحلَّل (لا ضياع).
- **تمّ عندما:** التصنيف صحيح + الثابت متوازن + لا عودة للمُعالَج.

## المرحلة 4 — موثوقية الذكاء الاصطناعي
- [ ] AI بزر يدوي فقط (لا في الإقلاع/الرسم)، بحدّ أقصى لكل ضغطة (موجود: 150).
- [ ] cache لنتائج AI (تفادي إعادة الطلب + التكلفة).
- [ ] عند فشل/غموض AI → «مراجعة» لا حذف. تأكد لكل مسارات AI.
- [ ] موجّهات (prompts) واضحة تُرجع نعم/لا حاسمة (تقليل الغموض).
- **تمّ عندما:** AI محافظ + مخزّن + إنسان في الحلقة قبل الإضافة.

## المرحلة 5 — الواجهة والعرض (تنظيم احترافي)
- [ ] كل قسم: صف إحصائيات واحد نظيف (لا تكرار)، فلاتر مطوية، أدوات ثانوية مطوية.
- [ ] بطاقات المنتجات: عرض موحّد (صورة/اسم/سعر/أقرب منتج لدينا/أزرار واضحة)، بلا أزرار معطوبة/مكررة.
- [ ] الفلاتر: متّسقة عبر الأقسام، سريعة (form بدل rerun لكل حرف).
- [ ] إزالة الأزرار غير المهمة/المكررة (بموافقة المستخدم — يرى الواجهة).
- [ ] HTML للبطاقات: لا أسطر فارغة (تُسرّب كنص في Streamlit) — استخدم `_no_blank_lines`.
- **تمّ عندما:** كل قسم نظيف منظّم، أزرار ضرورية فقط، عرض سريع متّسق.

## المرحلة 6 — التقوية النهائية + فحص شامل عند الحجم
- [ ] وسّع `tests/` لتغطي: المطابقة، التصنيف، التصدير، عدم التكرار، فشل AI.
- [ ] `pytest tests/ -q` كله أخضر.
- [ ] فحص الثابت (ledger) أخضر على تشغيل كامل.
- [ ] قياس أداء نهائي على البيانات الكاملة (بعد إضافة المنافسين/المنتجات).
- [ ] راجعة كود نهائية: لا `except: pass`، لا `print` (logging)، لا أرقام سحرية، لا كود ميت.
- **تمّ عندما:** كل الاختبارات + الثابت أخضر، والأداء مقبول عند الحجم الكامل.

---

## 📊 الحالة (يحدّثها المساعد بعد كل مرحلة)
- **المرحلة الحالية:** 1 (تقوية) — **H1 + L2 + M1 + M3 منجزة ومنشورة ومُتحقَّقة على Railway** ✅. الجاري الآن: **جانب الناتج** (تشخيص قراءة-فقط).
- **آخر تحديث:** 2026-06-16.
- **شبكة الأمان:** نسخة احتياطية `data/pricing_v18.backup.db` (مطابقة بايت-ببايت، 264,900,608) ✓. AST + `import app` ✓. `pytest tests/` = 9/9 أخضر ✓.
- **خط الأساس المقيس:** competitor_products_store = 129,644 صف · 23 منافساً · comp_catalog = 129,642 · our_catalog(DB)=7,795. (أداة baseline_missing مفقودة على master — تُنشأ عند الحاجة كسكربت مؤقت خارج git.)
- **تقرير التدقيق (مرتّب بالأولوية):**
  - ✅ **H1** ~~تحميل الجدول كاملاً (fetchall)~~ **مُصلَح** (commit 42a764e): بثّ fetchmany(5000) في `competitor_intelligence.py::find_missing_products`. مُتحقَّق على 129,644 صف: نتيجة متطابقة بايت-ببايت (118,170 مفقوداً)، ذروة RSS 317→290MB، والوفر يكبر مع الحجم (يحذف مخزن الإدخال الذي ينمو خطّياً). جانب الناتج (`app.py:961` per_page=1M = إعادة كل المفقودات) مؤجَّل لمرحلة مخصّصة.
  - ✅ **M1** ~~عتبات `config.py` غير موصولة~~ **مُصلَح** (8bcc6d9/e01dcde): `config` صار المصدر الوحيد لعتبتَي المفقودات (CONFIRMED=82 / REVIEW=65)؛ صُحّحت REVIEW 70→65 ثم وُصِلت في `_compute_missing_from_store`. مُخرَج متطابق بايت-ببايت. (BARRIER=85 غير مستخدم · مسار `engine.py` له عتباته المستقلّة 82/55/68 — خارج النطاق.)
  - ✅ **M2** ~~فشل صامت لقسم المفقودات~~ **مُصلَح** (commit bb7c3b5): logging.error + علَم session_state + st.warning يميّز الفشل عن «لا مفقود».
  - ✅ **M3** ~~`competitor_intake_ledger` = 0 صف رغم 129K منتج~~ **مُصلَح** (C1 49a77e5 / C2 1f4126f): السبب الجذري الأدقّ = البنية مُوصَّلة في `engine.py` (ingest+transition+sweep+invariant) لكن **لا منادي يحقن سجلّاً حقيقياً** — كل نداءات `run_full_analysis` تترك `ledger=None`→NullLedger (وليس «upsert_comp_catalog لا ينادي mark_ingested»). C1: إدخال مُجمّع `mark_ingested_batch` (executemany+commit واحد؛ 129,644 صف 22s→4.7s، صفوف متطابقة بايت-ببايت، 9/9 أخضر). C2: خانة «🔍 فحص الثابت» **مُطفأة افتراضياً** تحقن سجلّاً حقيقياً في **قاعدة SQLite مؤقّتة منفصلة** (نمو قاعدة الإنتاج=صفر، تُحذف بعد التقرير) وتعرض المحاسبة `مُدخَل==مطابق+مفقود+مرفوض+إعادة+أخطاء`. السجلّ مراقبة-فقط (df_all متطابق مع/بدون). مُتحقَّق على Railway.
  - ✅ **L1** ~~كود ميت `_auto_resolve_review`~~ **مُصلَح** (c16320b): حُذفت الدالة الميتة (150 سطراً) + تعليقاتها المعلّقة · ✅ **L2** ~~تضخّم فهارس~~ **مُصلَح** (bb5ae60/5bb9a7b/cf2448d): 16→8 فهارس، 3 مصادر موحّدة، إدخال أسرع 38% · ✅ **L5** ~~print تشخيصي~~ **مُصلَح** (5701586): تحويل إلى logging.info.
- **مُعالَج هذه الجلسة (المرحلة 1):** ✅ H1 — بثّ fetchmany بدل fetchall في `find_missing_products` (commit 42a764e). ✅ L2 — تنظيف الفهارس 16→8 عبر 3 commits ملف-واحد: C1 `db_manager` (bb5ae60) يُسقط الـ8 المكرّرة · C2 `competitor_intelligence` (5bb9a7b) يُفرّغ `_ensure_indexes` (لا يُمَسّ `_ensure_columns`) · C3 `mahally_scraper` (cf2448d) يُزيل خلق idx_cps_competitor/brand/price. كلها AST+import ✓، إدخال أسرع 38%، قياسات بسكربتات مؤقتة خارج git. ✅ M1 — وصل عتبتَي المفقودات إلى config عبر commitين: C1 `config` 70→65 (8bcc6d9) · C2 `app.py` وصل (e01dcde)، مُخرَج متطابق بايت-ببايت (assert 82/65). ✅ L1+L5 — حذف الدالة الميتة `_auto_resolve_review` (c16320b، 150 سطراً) + تحويل طباعة التشخيص إلى logging (5701586)، تنظيف بحت بلا تغيير سلوك.
- **مُنجَز سابقاً (جلسات 2026-06):** إصلاح تعليق Railway (نداء AI عند الإقلاع) · حجب بالهيكل العظمي للمفقودات · نقل المشبوه للمراجعة · عرض أقرب منتج لكل مفقود · تحقّق AI للمملوك باسم آخر · المُرسَل لا يعود (إطفاء الإرجاع + مفاتيح مستقرة) · تنظيم أوّلي لقسم المفقودات · إصلاح فحص تكرار المعالجة الذكية (_miss_bare). التفاصيل في git log.
- **ملاحظات/عوائق:** **M3 مُنجَز** (سجلّ ثابت عند الطلب). الجاري: **جانب الناتج** (`app.py:961` per_page=1M = إعادة كل المفقودات الفريدة — نمو خطّي في المخرَج، يمسّ عقد app↔engine). يتبقّى اختيارياً: توحيد عتبات مسار `engine.py` (82/55/68).
