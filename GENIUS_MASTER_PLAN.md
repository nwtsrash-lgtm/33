# الخطة الأمّ — نظام مهووس العبقري (دستور واحد)

> المرجع الموحّد. يجمع المسارين في خارطة طريق واحدة. التفاصيل في
> `DIAGNOSIS_UNIFIED_LOOP.md` (الدقّة) و`AUDIT_UI_BUTTONS.md` (الواجهة).
> توثيق فقط — لا يمسّ النشر. التاريخ: 2026-06-17.

---

## 0) القرار النهائي: **نُعدّل ونوحّد — لا نبني من جديد**

إعادة البناء ترمي 8 مراحل عمل + `app.py` (8,600 سطر) + نشراً يعمل + قاعدة
265MB + محرّك دائرة مغلقة **مكتوباً بالفعل**، وتُعيد نفس مشاكل التصميم
(التشتّت) في مشروع جديد. **المشكلة تشتّتٌ لا نقص ذكاء** — والحل توحيد.

---

## 1) المبادئ الثلاثة (ما نَعِد به بصدق)

1. **صفر فقدان بيانات — مضمون 100%.** قانون حفظ البيانات + السجل (Ledger):
   كل صف له مصير نهائي مُسجَّل، وفحص ثابت يصرخ عند أي تسرّب.
2. **صفر خطأ صامت أو لا رجعة فيه — مضمون 100%.** عند الشك → «مراجعة»
   (يبقى ظاهراً)؛ لا حذف؛ إنسان في الحلقة قبل الإرسال.
3. **دقّة المطابقة تقترب من 100% عبر التعلّم — لا تُدّعى حرفياً.** كل تصحيح
   بشري يُكتب فيمنع تكرار الخطأ. (من يَعِدك بـ«صفر خطأ في الحكم» يكذب.)

---

## 2) خارطة الطريق — مساران متوازيان مرتّبان بالخطر

### المسار (أ) — الحلقة المغلقة والدقّة [قلب طلبك]

| م | العمل | ملف | خطر | حالة |
|---|---|---|---|---|
| أ0 | ~~توحيد المُطبِّع~~ | — | — | ❌ **مرفوض** بعد قراءة الكود: `normalize_name` يحذف الحجم/النوع اللذين يحتاجهما الفحص الهيكلي → انحدار في الدقّة |
| أ1 | مركزة العتبات في config | — | — | ✅ مُنجزة سلفاً |
| أ2 | ~~Ledger دائماً في المسار الحيّ~~ | — | — | ❌ **مرفوض**: يخالف قرار M3 (Ledger عند الطلب لأمان OOM على 130K صف)؛ وصفر-الفقدان **مضمون هيكلياً أصلاً** (`_row` يُعيد صفاً لكل منتج — لا إسقاط صامت) |
| أ3 | بوابة التحقّق الشديد قبل البطاقة (هيكل: حجم/ماركة) | `engine.py` | متوسط | ✅ **مُطبّق محلياً** `1b9a94a` (غير مدفوع) عبر `_structural_match` في `_row` |
| أ4 | الحلقة المستمرة بزر يدوي + دفعات | — | — | ✅ **مبنيّة سلفاً ومُحصَّنة**: `run_analysis_background_job` خيط خلفي + حفظ كل 50/200 + نتائج جزئية حيّة + watchdog + حماية صفوف وهمية |
| أ5 | ذاكرة التعلّم (تصحيح بشري → مرادف/ماركة/عتبة) | `app.py` | متوسط | 🔎 المرشّح الوحيد المتبقّي (قد يوجد جزئياً عبر `force_links`) |

### المسار (ب) — الواجهة والعرض والتنظيف [ما تبقّى]

| م | العمل | خطر | حالة |
|---|---|---|---|
| ب1 | توحيد مداخل «إرسال لـ Make» (~8 → مدخل واضح/سياق) | متوسط | مقترح |
| ب2 | إزالة تكرار الفلاتر (3×) + دمج أزرار AI (~9) | منخفض | مقترح |
| ب3 | تطوير البطاقات والعرض والمساحات والخيارات | منخفض–متوسط | مقترح |
| ب4 | توضيح دورة المنتج بين الأقسام (تعديل→Make→تمت المعالجة) | منخفض | مقترح |
| ب5 | إعادة تنظيم صفحة المفقودات المتضخّمة (4570–6262) | **عالٍ** | **أخيراً** |

---

## 3) ملاحظات أمانة (ما هو آمن وما هو خطر — مُتحقَّق)

- ✅ **لوحة تحكّم الكشط حيّة ونظيفة** (إضافة/حذف متجر + جدولة + تشغيل/إيقاف،
  `app.py:7440–7648`) — **لا تُحذف**. كادت تُصنَّف ميتة خطأً.
- ⚠️ **ملفّات الكاشطات متشابكة** (`browser_like_http`/`selenium_scraper_v30`
  مستوردة؛ `async_scraper.py` في 4 مجلّدات) — **حذف الملفّات ليس آمناً**؛
  يُؤجَّل ويُدرَس بعناية، ليس أولوية.
- ⚠️ **ازدواج آليتي كشط**: `_start_scraper_bg`→سكربت `async_scraper.py`
  (زر 7641) مقابل `run_advanced_price_scraping` (زر 8217) — **يُتحقَّق** هل
  هما لغرضين مختلفين قبل أي دمج.
- 🔴 **صفحة المفقودات** أعلى قيمة لكنها المسار الحيّ الحسّاس للفقدان —
  تُعالَج **أخيراً** بعد إثبات سير العمل على خطوات أأمن.
- 🧠 **إعادة تأطير (2026-06-17):** بعد الجرد، **معظم الأزرار حيّة وتعمل** —
  الفوضى من كثرة المداخل وضعف التنظيم البصري، لا من كود ميّت. فالعلاج =
  **توحيد المداخل + تحسين الهرمية والعرض**، لا حذف جماعي.
- ❌ **أ0 مرفوضة:** المُطبِّعان مختلفان بالقصد (`normalize_text` يحفظ
  الحجم/النوع للفحص الهيكلي؛ `normalize_name` يحذفهما لمطابقة المفقودات).
  تبديلهما = انحدار. تحسين دقّة reconciliation الصحيح = إثراء مرادفات
  `normalize_text` دون حذف الهيكل (تغيير دقيق لاحق، ليس أوّلاً).

---

## 4) طريقة التنفيذ (مُتّفق عليها)

- **Claude يعدّل مباشرة**، ملف واحد لكل commit، يعرض الفرق.
- **المالك وحده يدفع ويتحقّق على Railway**، يؤكّد «يعمل»، ثم الخطوة التالية.
- **جلسة مستمرة** (لا محادثة جديدة لكل إصلاح)؛ هذه الوثائق + الذاكرة =
  «نقاط حفظ» تنقل السياق لو بدأنا جلسة جديدة.
- قبل كل commit: `ast.parse` + `import app` بلا أخطاء.
- لا إعادة هيكلة كود، لا ملفّات جديدة إلا لضرورة، رسائل عربية.

---

## 5) أول commit مقترح (مُصحَّح بعد قراءة الكود)

أ0 رُفِضت (انحدار). الأول الصحيح = **مكسب عرضٍ آمن (المسار ب3): تحسين بصري
للبطاقات/المساحات** — لمسة العرض فقط، **بلا أي منطق بيانات/مطابقة**:
- خطر شبه معدوم (تقديم فقط).
- **مرئي فوراً** — تتحقّق منه بعينك دون خبرة Railway.
- يخدم رغبتك «تطوير العرض والبطاقات» مباشرة.
- يثبت حلقة (أعدّل → تدفع → تتحقّق) على شيء آمن قبل المسارات الحسّاسة.

ثم ننتقل للمسار (أ) الحسّاس خطوة-خطوة مع تحقّقك عند كل حدّ. **بانتظار «ابدأ ب3».**
